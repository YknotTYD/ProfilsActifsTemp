[project]
name = "profilsactifs"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { email = "pierre.maciejewski@epitech.eu" }
]
requires-python = ">=3.14"
dependencies = [
    "django>=6.1",
    "gunicorn>=26.2.0",
    "psycopg[binary]>=3.3.4",
]

[project.scripts]
profilsactifs = "profilsactifs:main"

[build-system]
requires = ["uv_build>=0.12.7,<0.13.0"]
build-backend = "uv_build"
