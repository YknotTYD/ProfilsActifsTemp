# Cle secrete Django (obligatoire) -- voir README, section Configuration :
#   python3 -c "import secrets; print(secrets.token_urlsafe(50))"
DJANGO_SECRET_KEY=

# Port publie par docker compose (8080 par defaut)
# PORT=8080
